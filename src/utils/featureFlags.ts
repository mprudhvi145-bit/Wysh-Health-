
export const features = {
  enable3D: true,
  enableAI: true, // Restricted to summaries
  enableVitals: true, // Sandbox mode
  enableNotifications: true,
  mockMode: true, // Enforce Sandbox for Pilot
  debug: false,
  pilotMode: true // Global Pilot Flag
};
